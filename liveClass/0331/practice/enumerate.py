def main():
    numbers = ['zero', 'one', 'two', 'three', 'four', 'five']

    # numbers를 인덱스와 값을 이용해 출력해보세요.
    for idx, nums in enumerate(numbers):
        print(f'{idx} is {nums}')


if __name__ == "__main__":
    main()
